/**
 * Where this lives, and the only place that says so.
 *
 * The metadata, the canonical link, `robots.txt` and the sitemap all have to name the same
 * origin, and three copies of a URL is three chances to move house and forget one. A
 * sitemap advertising an address the canonical link disagrees with is worse than having no
 * sitemap, because a crawler then has to pick.
 */
export const SITE_URL = "https://chess.sanyam.sh";

export const SITE_NAME = "sixtyfour";

export const SITE_DESCRIPTION = "A very small chess game. Play the bot at three difficulties.";
